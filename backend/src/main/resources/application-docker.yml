# Docker-specific configuration
spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/ourride_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:root}
  
  redis:
    host: ${REDIS_HOST:redis}
    port: ${REDIS_PORT:6379}

